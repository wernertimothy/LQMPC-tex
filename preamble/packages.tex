
% math
\usepackage{amssymb, amsmath}

% color
\usepackage{color, xcolor}

% hyperref
%\usepackage[linktocpage=true]{hyperref}  % use this to only link the page number
\usepackage[linktoc=all]{hyperref}       % use this to link the whole expression

% margins
\usepackage{geometry}

% frames
\usepackage{mdframed}

% sections
\usepackage{sectsty}
\usepackage{titlesec}

% foots n heads
\usepackage{fancyhdr}