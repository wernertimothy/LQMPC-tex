
\section{The linear-quadratic OCP}
The finite-horizon linear-quadratic optimal control problem is formulated as follows.

\[
\begin{aligned}
& \underset{x_k, u_k}{\text{minimize}} & & \sum_{k=0}^{N} \frac{1}{2} \vxuone{k}\T \Hcoststacked{k} \vxuone{k} & &                      \\
& \text{subject to}                    & & x_0 = \hat{x}                                                       & &                      \\
&                                      & & x_{k+1} = A_k x_k + B_k u_k + b_k                                   & & ,k\in\Iset{0}{N-1}   \\
&                                      & & \underline{x}_k \leq J_k^{bx} x_k \leq \bar{x}_k                    & & ,k\in\Iset{0}{N}     \\
&                                      & & \underline{u}_k \leq J_k^{bu} u_k \leq \bar{u}_k                    & & ,k\in\Iset{0}{N}     \\
&                                      & & C_k x_k + D_k u_k \leq d_k                                          & & ,k\in\Iset{0}{N}
\end{aligned}
\]

With the states $x_k \in\Xset\subseteq\R{n_x}$ and inputs $u_k \in\Uset\subseteq\R{n_u}$.
The state and input dimensions are $n_x$ and $n_u$, respectively.
The time horizon is $N \in \mathbb{N}_{>0}$ and the initial state is given as $\hat{x} \in \Xset$.
The system dynamics are described by the matrices $A_k \in \R{n_x \times n_x}$, $B_k \in \R{n_x \times n_u}$ and the vector $b_k \in \R{n_x}$.
The cost function is defined by the state weight matrix $Q_k \in \Sp{n_x}$, which is required to be symmetric positive-semi definite.
The input weight matrix $R_k$, which is required to be symmetric and positive-definite on the control subspace $R_k - S_k\T Q_k^{-1} S_k \in \Spp{n_u \times n_u}$, and the cross-term weight matrix $S_k \in \R{n_u \times n_x}$.
The linear terms of the cost function are given by the vectors $q_k \in \R{n_x}$ and $r_k \in \R{n_u}$.
There are a number of $n_{bx}$ box constraints on the states and $n_{bu}$ box constraints on the inputs, which are mapped by the selection matrices $J_k^{bx}$ and $J_k^{bu}$, together with the lower bounds $\underline{x}_k$, $\underline{u}_k$ and upper bounds $\bar{x}_k$, $\bar{u}_k$ respectively.  
There are a number of $n_p$ polytopic constraints represemted by the matrices $C_k\in\R{n_x \times n_p}$, $D_k\in\R{n_u \times n_p}$ and the vector $d_k\in\R{n_p}$.
The integer set is denoted as $\Iset{a}{b} := \lbrace a, a+1, \dots b \rbrace$.

\newpage